# Hex-Feinschliff Runde 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fünf User-Reports: Zugverfall bei Fehlversuch (Hex), Licht bis zur unteren Hex-Reihe, korrektes PSG-Logo, Hex-Label-Umbruch, DFB-Pokal-Daten (Wirtz/Kane) inkl. strukturellem Query-Fix.

**Architecture:** Game.jsx-Fehlerzweig schreibt jetzt einen Zugwechsel (wie im Raster); CSS-Anpassungen an `.board::before`/`.hexLabel`; `fetch_logos.mjs` bekommt Team-ID-Overrides mit Lookup-Verifikation; `wikidata_honours.mjs` bekommt P580/P585-COALESCE + exportierte `HONOUR_OVERRIDES`; neues `apply_honour_overrides.mjs` wendet die Overrides sofort auf `players.js` an.

**Tech Stack:** unverändert.

---

## Task 1: Hex — Fehlversuch kostet den Zug

**Files:**
- Modify: `src/Game.jsx`

- [ ] **Step 1:** In `handleSubmit` den Block

```jsx
    if (results[selected] !== true) {
      setLocalFeedback({ type: "err", text: `${player.n} passt nicht zu „${cname(board[selected].def)}".`,
        detail: "Wähle ein Feld, das zur Karriere des Spielers passt (Verein, Nation, Liga, Titel oder Spezialfeld)." });
      play("err");
      return;
    }
```

ersetzen durch:

```jsx
    if (results[selected] !== true) {
      setLocalFeedback({ type: "err", text: `${player.n} passt nicht zu „${cname(board[selected].def)}".`,
        detail: "Zug verfällt — der Gegner ist dran." });
      play("err");
      setSelected(null); setNameInput(""); setChosen(null); setSugOpen(false);
      const rem = liveRemaining(clk, myPlayer, Date.now());
      writeMove({ turn: myPlayer === 1 ? 2 : 1,
        clocks: { ...clk, [myPlayer]: rem, started: new Date().toISOString() },
        last_move: { by: 0, text: `${names[myPlayer]}: ${player.n} passt nicht — Zug verfällt.`, claimed: [], ts: Date.now() } });
      return;
    }
```

- [ ] **Step 2:** Regel-Modal ergänzen — die Zeile

```jsx
            <p className="ruleP">Ab <b>2 Buchstaben des Nachnamens</b> erscheinen Treffer aus der Datenbank. Wähle einen Spieler aus der Liste.</p>
```

davor einfügen:

```jsx
            <p className="ruleP">Passt der genannte Spieler nicht, <b>verfällt der Zug</b> — der Gegner ist dran.</p>
```

- [ ] **Step 3:** `npm run build` (Expected `✓`), dann Commit:

```bash
git add src/Game.jsx
git commit -m "fix: Hex — Fehlversuch kostet den Zug (wie im Raster)"
```

---

## Task 2: CSS — Lichtkegel + Label-Umbruch

**Files:**
- Modify: `src/styles.css`

- [ ] **Step 1:** `.board::before` ersetzen:

```css
.board::before { /* Flutlicht-Pool hinter dem Brett — deckt auch die unterste Reihe */
  content: ""; position: absolute; inset: -12% -8% -14%; z-index: 0; pointer-events: none;
  background: radial-gradient(65% 58% at 50% 46%, rgba(244,201,93,.12), transparent 78%);
}
```

Und `.board`-Margin: `margin: 20px auto 6px;` → `margin: 20px auto 14px;`

- [ ] **Step 2:** `.hexLabel` ersetzen:

```css
.hexLabel { font-family: 'Hanken Grotesk', sans-serif; font-weight: 800; letter-spacing: .01em; text-align: center; font-size: clamp(7.5px, 2.6vw, 11px); line-height: 1.06; text-shadow: 0 1px 3px rgba(0,0,0,.65); max-width: 100%; overflow-wrap: anywhere; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
```

- [ ] **Step 3:** `npm run build` + Commit:

```bash
git add src/styles.css
git commit -m "fix: Flutlicht deckt untere Hex-Reihe; Labels zweizeilig geclampt"
```

---

## Task 3: PSG-Logo via Team-ID

**Files:**
- Modify: `data-pipeline/fetch_logos.mjs`

- [ ] **Step 1:** Nach der `CLUB_SEARCH`-Tabelle einfügen:

```js
// Direkte Team-IDs für Fälle, in denen die Namenssuche fehlgreift
// (PSG-Suche liefert „Torcy"). Verifikation: strTeam muss Namensbestandteil enthalten.
const TEAM_ID = { PSG: [133714, "Paris"] };
```

- [ ] **Step 2:** In der Club-Schleife den `try`-Anfang erweitern — vor der `searchteams`-Zeile:

```js
      if (TEAM_ID[key]) {
        const [id, mustContain] = TEAM_ID[key];
        const j = await getJson(`${API}/lookupteam.php?id=${id}`);
        const t = j.teams?.[0];
        if (!t || !(t.strTeam || "").includes(mustContain) || !t.strBadge) { miss.push(`club ${key} (id ${id}): Verifikation fehlgeschlagen`); continue; }
        await download(t.strBadge, join(OUT, "club", key + ".png"));
        console.log(`  club ${key}: ${t.strTeam} (per ID) ✓`);
        await sleep(2500);
        continue;
      }
```

(Die ID-Abfrage steht VOR dem `existsSync`-Skip? Nein — Skip bleibt zuerst; für den Neu-Download wird die PSG-Datei in Step 3 gelöscht.)

- [ ] **Step 3: PSG neu laden**

```bash
rm public/logos/club/PSG.png && node data-pipeline/fetch_logos.mjs 2>&1 | grep -v übersprungen
```

Expected: `club PSG: Paris Saint-Germain (per ID) ✓`, `file public/logos/club/PSG.png` = PNG.

- [ ] **Step 4: Commit**

```bash
git add data-pipeline/fetch_logos.mjs public/logos/club/PSG.png
git commit -m "fix: PSG-Logo — Namenssuche traf Torcy, jetzt verifizierter ID-Lookup"
```

---

## Task 4: DFB-Pokal-Daten — Query-Fix + Overrides

**Files:**
- Modify: `data-pipeline/wikidata_honours.mjs`
- Create: `data-pipeline/apply_honour_overrides.mjs`
- Modify (generiert): `src/players.js`

- [ ] **Step 1: Query-Fix.** In `data-pipeline/wikidata_honours.mjs` in `fetchHonourPlayers` die Query-Zeilen

```js
      ?season wdt:P3450 wd:${qid} ; wdt:P1346 ?winner ; wdt:P580 ?ss .
      FILTER( YEAR(?ss) >= ${from} && YEAR(?ss) < ${to} )
      OPTIONAL { ?season wdt:P582 ?se. }
```

ersetzen durch (Saisonjahr aus P580 ODER P585 — Pokal-Saisons haben oft kein Startdatum):

```js
      ?season wdt:P3450 wd:${qid} ; wdt:P1346 ?winner .
      OPTIONAL { ?season wdt:P580 ?st580. }
      OPTIONAL { ?season wdt:P585 ?st585. }
      BIND(COALESCE(?st580, ?st585) AS ?ss)
      FILTER( BOUND(?ss) && YEAR(?ss) >= ${from} && YEAR(?ss) < ${to} )
      OPTIONAL { ?season wdt:P582 ?se. }
```

- [ ] **Step 2: Overrides-Tabelle.** In `data-pipeline/wikidata_honours.mjs` nach `COMP_QID` einfügen:

```js
// Kuratierte, vom Owner bestätigte Fakten, die Wikidata (noch) nicht liefert.
// Key: norm(name)|Geburtsjahr -> zusätzliche Honour-Keys (additiv).
export const HONOUR_OVERRIDES = {
  "florian wirtz|2003": ["DFB"],  // DFB-Pokal 2024 mit Leverkusen
  "harry kane|1993": ["DFB"],
};
```

Und in `main()` nach der Zuweisungs-Schleife (`else delete p.t;`-Block) einfügen:

```js
  // Kuratierte Overrides additiv anwenden
  for (const p of players) {
    const extra = HONOUR_OVERRIDES[norm(p.n) + "|" + p.by];
    if (extra) p.t = [...new Set([...(p.t || []), ...extra])].sort();
  }
```

- [ ] **Step 3: Sofort-Anwendung.** `data-pipeline/apply_honour_overrides.mjs` erstellen:

```js
#!/usr/bin/env node
/* Wendet HONOUR_OVERRIDES aus wikidata_honours.mjs sofort auf src/players.js an
   (additiv, alle übrigen Felder unverändert). Kein Netz nötig. */
import { readFileSync, writeFileSync } from "fs";
import { fileURLToPath, pathToFileURL } from "url";
import { dirname, join } from "path";
import { HONOUR_OVERRIDES, norm } from "./wikidata_honours.mjs";
import { stampDataInfo } from "./stamp.mjs";

const HERE = dirname(fileURLToPath(import.meta.url));
const PLAYERS_PATH = join(HERE, "..", "src", "players.js");

function recToString(r) {
  let s = `{"n": ${JSON.stringify(r.n)}, "ln": ${JSON.stringify(r.ln)}, "by": ${r.by}, "nat": ${JSON.stringify(r.nat)}, "clubs": ${JSON.stringify(r.clubs)}`;
  if (r.t && r.t.length) s += `, "t": ${JSON.stringify(r.t)}`;
  if (r.sl) s += `, "sl": ${r.sl}`;
  if (r.pos) s += `, "pos": ${JSON.stringify(r.pos)}`;
  if (r.cp && r.cp.length) s += `, "cp": ${JSON.stringify(r.cp)}`;
  return s + "}";
}

const mod = await import(pathToFileURL(PLAYERS_PATH).href + "?t=" + Date.now());
const players = mod.PLAYERS.map((p) => ({ ...p }));
let hits = 0;
for (const p of players) {
  const extra = HONOUR_OVERRIDES[norm(p.n) + "|" + p.by];
  if (!extra) continue;
  const before = (p.t || []).join(",");
  p.t = [...new Set([...(p.t || []), ...extra])].sort();
  if (p.t.join(",") !== before) { hits++; console.log(`  ${p.n}: t = [${p.t}]`); }
}
players.sort((a, b) => a.n.localeCompare(b.n, "en"));
const header = readFileSync(PLAYERS_PATH, "utf8").split("export const PLAYERS")[0];
writeFileSync(PLAYERS_PATH, header + "export const PLAYERS = [\n  " + players.map(recToString).join(",\n  ") + "\n];\n");
stampDataInfo();
console.log(`Fertig: ${hits} Spieler ergänzt.`);
```

**Hinweis:** Der Import führt das Honours-Modul-Top-Level aus (nur Konstanten; `main()` ist guard-geschützt).

- [ ] **Step 4: Anwenden + verifizieren**

```bash
node data-pipeline/apply_honour_overrides.mjs
git diff --stat src/players.js
node -e 'import("./src/players.js").then(({PLAYERS})=>{["Florian Wirtz","Harry Kane"].forEach(n=>{const p=PLAYERS.find(x=>x.n===n);console.log(n,p.t)})})'
```

Expected: 2 Treffer; Diff = 2 geänderte Spieler-Zeilen (+ dataInfo.js); beide `t` enthalten `DFB`.

- [ ] **Step 5: Tests + Commit**

```bash
npm test && git add data-pipeline/wikidata_honours.mjs data-pipeline/apply_honour_overrides.mjs src/players.js src/dataInfo.js
git commit -m "fix: DFB-Pokal-Daten — P580/P585-Query-Fix + kuratierte Overrides (Wirtz, Kane)"
```

---

## Task 5: Verifikation & Abschluss

- [ ] **Step 1:** `npm test` (42 grün) + `npm run build` (`✓`).
- [ ] **Step 2:** `superpowers:finishing-a-development-branch` — Push + PR, mergen auf Zuruf.

---

## Self-Review-Ergebnis

- **Spec-Abdeckung:** Report 1 → Task 1; Report 2+4 → Task 2; Report 3 → Task 3; Report 5 → Task 4 (Query-Fix + Overrides + Sofort-Anwendung). Keine Lücke.
- **Platzhalter:** keine.
- **Typ-/Namenskonsistenz:** `HONOUR_OVERRIDES`-Format `norm(n)|by → [Keys]` identisch in honours-Skript und apply-Skript; `recToString` mit pos+cp (aktueller Vollstand); PSG-`TEAM_ID`-Verifikation analog QID-Muster.
